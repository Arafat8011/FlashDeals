type productsTypes = {
  id: string,
  name: string
  brand: string
  price: number
  currency: string
  stock: number,
  rating: number,
  description: string,
  image: string,
  specifications: {
    processor: string,
    ram: string,
    storage: string,
    display: string
  }
}
